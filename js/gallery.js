$('.header__menu-btn').on('click', function () {
    $('.header__menu-btn').toggleClass('header__menu-btn_close');
  });
  $('.header__menu-btn').on('click', function () {
    $('.header__inner-items').toggleClass('header__inner-items-active');
  });